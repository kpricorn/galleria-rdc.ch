---
title: archive
pageable: true
layout: layouts/aktuell_summary
---

%h3
  Archiv
  - case page_type
  - when 'day'
    = Date.new(year, month, day).strftime('%b %e %Y')
  - when 'month'
    = Date.new(year, month, 1).strftime('%b %Y')
  - when 'year'
    = year
%ul
  - page_articles.each_with_index do |article, i|
    %li
      = link_to article.title, article
      %span= article.date.strftime('%b %e')

- if paginate
  %hr
  .pagination-centered
    %ul.pagination
      - if prev_page
        %li.arrow
          = link_to '«', prev_page
      %li
        Seite #{page_number} von #{num_pages}
      - if next_page
        %li.arrow
          = link_to '»', next_page
